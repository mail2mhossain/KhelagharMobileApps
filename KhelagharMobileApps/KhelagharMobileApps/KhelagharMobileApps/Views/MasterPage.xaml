<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="KhelagharMobileApps.Views.MasterPage">

    <MasterDetailPage.Master>
        <NavigationPage Title="Khelaghar" Icon="hamburger.png">
            <x:Arguments>
                <ContentPage Title="Menu">
                    <StackLayout Padding="40">
                        <Label Text="{Binding UserName,StringFormat='Hello, {0}'}" />
                        <Button Margin="10.0" Text="Asar Search" Command="{Binding NavigateCommand}" CommandParameter="NavigationPage/MainPage" />
                        <Button Margin="10.0" Text="Logout" Command="{Binding NavigateCommand}" CommandParameter="/Login" />
                    </StackLayout>
                </ContentPage>
            </x:Arguments>
        </NavigationPage>
    </MasterDetailPage.Master>
    
</MasterDetailPage>